SET QUOTED_IDENTIFIER ON
GO
SET ANSI_NULLS ON
GO




CREATE VIEW [dbo].[CHCNWEB_SPEC_LANGUAGE_VS]
AS
SELECT DISTINCT TOP 100 PERCENT PROVID, [LANGUAGE]
FROM         dbo.CHCNWEB_SPECIALISTS
WHERE     (NOT ([LANGUAGE] IS NULL))
ORDER BY PROVID, [LANGUAGE]




GO
