SET QUOTED_IDENTIFIER ON
GO
SET ANSI_NULLS ON
GO



create PROCEDURE [dbo].[CHCNPORTAL_MAKE_CLAIM_NOTES]

AS

-- RUNS NIGHTLY, 01/22/2014, CTA
	
if exists (select * from dbo.sysobjects where id = object_id(N'[dbo].[CHCNPORTAL_CLAIM_NOTES]') and OBJECTPROPERTY(id, N'IsUserTable') = 1)
DROP TABLE [dbo].[CHCNPORTAL_CLAIM_NOTES]

SELECT   CLAIMNO, Subject, Notes
INTO DBO.CHCNPORTAL_CLAIM_NOTES
FROM     EZCAP65TEST.EZCAPDB.dbo.CLAIM_NOTES WITH(NOLOCK)

CREATE INDEX CLAIMNO on CHCNPORTAL_CLAIM_NOTES(CLAIMNO)
























GO
